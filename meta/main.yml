---
galaxy_info:
  author: MongoDB
  role_name: local-tap
  description: Installs Brew packages from local tap
  license: Apache
  min_ansible_version: 2.8
  platforms:
    - name: debian
      versions:
        - buster
  galaxy_tags:
    - local-tap
